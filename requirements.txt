# Core app dependencies
streamlit>=1.33

# Scientific stack
numpy==1.26.4
pandas==2.1.4
scikit-learn==1.3.2

# Retrieval & ML
faiss-cpu
rank-bm25

# Transformers & embeddings
transformers>=4.41
sentence-transformers>=2.5
torch==2.2.2
accelerate
peft

# HuggingFace datasets (fixed versions to avoid pyarrow crash)
datasets==2.16.1
pyarrow==14.0.2

# TensorFlow wrapper (lightweight)
tf-keras
